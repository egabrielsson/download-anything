[build]
builder = "nixpacks"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["yt-dlp", "ffmpeg"]

[deploy]
startCommand = "cd backend && go run ./cmd/server"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
